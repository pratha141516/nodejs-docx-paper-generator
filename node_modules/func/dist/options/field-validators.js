"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ValueValidate = exports.Exclusive = exports.DependsOn = exports.Enum = exports.Required = void 0;
require("reflect-metadata");
const errors_1 = require("../errors");
const metadata_1 = require("../utils/metadata");
const validator = __importStar(require("../utils/validator"));
const throwInvalidFieldTarget = (propertyKey) => {
    throw (0, errors_1.createSystemError)(errors_1.F_SYSTEM.INVALID_PARAM_TYPE, errors_1.errorTypes.DEFINITION, `Field validator "${String(propertyKey)}" must decorate an instance property.`, { property: String(propertyKey) });
};
const Required = () => (target, propertyKey) => {
    if (typeof target === 'function' || typeof propertyKey !== 'string') {
        throwInvalidFieldTarget(propertyKey);
    }
    const key = propertyKey;
    const requiredKeys = Reflect.getMetadata(metadata_1.metadata.REQUIRED_FIELD_IDENTIFIER, target.constructor) || [];
    Reflect.defineMetadata(metadata_1.metadata.REQUIRED_FIELD_IDENTIFIER, requiredKeys.includes(key) ? requiredKeys : requiredKeys.concat([key]), target.constructor);
    const options = Reflect.getMetadata(metadata_1.metadata.FIELD_OPTION_IDENTIFIER, target.constructor) || [];
    Reflect.defineMetadata(metadata_1.metadata.FIELD_OPTION_IDENTIFIER, options.map((item) => {
        if (item.propertyKey !== key)
            return item;
        return Object.assign({}, item, { required: true });
    }), target.constructor);
};
exports.Required = Required;
const Enum = (values) => (target, propertyKey) => {
    validator.mustBeArray(values, 'enum');
    defineFieldConstraint(target, propertyKey, { enum: values });
};
exports.Enum = Enum;
const DependsOn = (options) => (target, propertyKey) => {
    validator.mustBeArray(options, 'dependsOn');
    options.forEach(item => validator.optionName(item, 'dependsOn'));
    defineFieldConstraint(target, propertyKey, { dependsOn: options });
};
exports.DependsOn = DependsOn;
const Exclusive = (options) => (target, propertyKey) => {
    validator.mustBeArray(options, 'exclusive');
    options.forEach(item => validator.optionName(item, 'exclusive'));
    defineFieldConstraint(target, propertyKey, { exclusive: options });
};
exports.Exclusive = Exclusive;
const ValueValidate = (fn) => (target, propertyKey) => {
    if (typeof target === 'function' || typeof propertyKey !== 'string') {
        throwInvalidFieldTarget(propertyKey);
    }
    const key = propertyKey;
    const validators = Reflect.getMetadata(metadata_1.metadata.VALUE_VALIDATOR_IDENTIFIER, target.constructor) || {};
    Reflect.defineMetadata(metadata_1.metadata.VALUE_VALIDATOR_IDENTIFIER, Object.assign({}, validators, {
        [key]: (validators[key] || []).concat([fn]),
    }), target.constructor);
};
exports.ValueValidate = ValueValidate;
const defineFieldConstraint = (target, propertyKey, params) => {
    if (typeof target === 'function' || typeof propertyKey !== 'string') {
        throwInvalidFieldTarget(propertyKey);
    }
    const key = propertyKey;
    const constraints = Reflect.getMetadata(metadata_1.metadata.FIELD_CONSTRAINT_IDENTIFIER, target.constructor) || {};
    const nextParams = Object.assign({}, constraints[key] || {}, params);
    Reflect.defineMetadata(metadata_1.metadata.FIELD_CONSTRAINT_IDENTIFIER, Object.assign({}, constraints, { [key]: nextParams }), target.constructor);
    const options = Reflect.getMetadata(metadata_1.metadata.FIELD_OPTION_IDENTIFIER, target.constructor) || [];
    Reflect.defineMetadata(metadata_1.metadata.FIELD_OPTION_IDENTIFIER, options.map((item) => {
        if (item.propertyKey !== key)
            return item;
        return Object.assign({}, item, nextParams);
    }), target.constructor);
};
