import { OptionParams, RegisterCommandParams } from '../interfaces';
export declare class RegisterProvider {
    private _commands;
    private _options;
    static isRegisterProvider: boolean;
    constructor(_commands?: RegisterCommandParams[], _options?: OptionParams[]);
    get commands(): RegisterCommandParams[];
    get options(): OptionParams[];
}
