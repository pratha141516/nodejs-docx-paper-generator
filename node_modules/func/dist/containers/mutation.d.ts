import { CommandClass, OptionClass } from '../interfaces';
export interface DevourData {
    commands: CommandClass[];
    options: OptionClass[];
    missing: CommandClass[];
    majors: CommandClass[];
}
export declare class Mutation {
    private argv;
    private args;
    constructor(argv?: string[]);
    devour({ commands, options, missing, majors }: DevourData): any;
    private findSubOptions;
    private findCommand;
    private throwUnknownOptions;
}
