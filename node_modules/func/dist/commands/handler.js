"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Handler = void 0;
require("reflect-metadata");
const errors_1 = require("../errors");
const validator = __importStar(require("../utils/validator"));
const metadata_1 = require("../utils/metadata");
const Handler = (params = {}) => (target, propertyKey) => {
    if (typeof target === 'function' || typeof propertyKey !== 'string') {
        throw (0, errors_1.createSystemError)(errors_1.F_SYSTEM.INVALID_PARAM_TYPE, errors_1.errorTypes.DEFINITION, `Handler "${String(propertyKey)}" must decorate an instance method.`, { method: String(propertyKey) });
    }
    if (params.path && params.alias) {
        throw (0, errors_1.createSystemError)(errors_1.F_SYSTEM.INVALID_PARAM_VALUE, errors_1.errorTypes.DEFINITION, 'Handler path does not support alias.', { alias: params.alias, path: params.path });
    }
    if (params.flag && params.path) {
        throw (0, errors_1.createSystemError)(errors_1.F_SYSTEM.INVALID_PARAM_VALUE, errors_1.errorTypes.DEFINITION, 'Handler flag and path cannot be used together.', { flag: params.flag, path: params.path });
    }
    if (!params.flag && params.alias) {
        throw (0, errors_1.createSystemError)(errors_1.F_SYSTEM.MISSING_REQUIRED_PARAM, errors_1.errorTypes.DEFINITION, 'Handler alias requires a flag.', { key: 'flag', alias: params.alias });
    }
    if (params.path) {
        validator.mustBeArray(params.path, 'path');
        params.path.forEach(item => validator.commandName(item, 'path'));
    }
    if (Object.prototype.hasOwnProperty.call(params, 'flag')) {
        validator.optionName(params.flag || '', 'flag');
    }
    validator.optionAlias(params.alias, 'alias');
    const handlers = Reflect.getMetadata(metadata_1.metadata.METHOD_HANDLER_IDENTIFIER, target.constructor) || [];
    Reflect.defineMetadata(metadata_1.metadata.METHOD_HANDLER_IDENTIFIER, handlers.concat([
        Object.assign({}, params, {
            methodName: propertyKey,
        }),
    ]), target.constructor);
};
exports.Handler = Handler;
