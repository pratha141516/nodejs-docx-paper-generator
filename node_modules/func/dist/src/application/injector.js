"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ServiceInjector = void 0;
class ServiceInjector {
    services;
    instances = new Map();
    constructor(services = []) {
        this.services = services;
    }
    resolve(target, resolveParams, serviceStack = []) {
        if (!target || !this.services.includes(target))
            return undefined;
        if (this.instances.has(target))
            return this.instances.get(target);
        if (serviceStack.includes(target))
            return undefined;
        const nextStack = serviceStack.concat([target]);
        const instance = new target(...resolveParams(target, nextStack));
        this.instances.set(target, instance);
        return instance;
    }
}
exports.ServiceInjector = ServiceInjector;
