import type arg from 'arg';
import { FieldOptionParams, UserOption } from '../interfaces';
export declare class ConstraintValidator {
    private fieldOptions;
    private args;
    private option;
    constructor(fieldOptions: FieldOptionParams[], args: arg.Result<any>, option: UserOption);
    validate(): void;
    private explicitNames;
    private validateDependsOn;
    private validateEnum;
    private validateExclusive;
}
