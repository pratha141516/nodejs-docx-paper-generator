import arg from 'arg';
import { CommandClass, HandlerParams, RegisterCommandParams, UserOption } from '../interfaces';
import type { ServiceClass } from '../application/interfaces';
import { FuncException } from '../context';
export interface FactoryParams {
    args: arg.Result<any>;
    nativeOption: UserOption;
    commands: CommandClass[];
    command?: RegisterCommandParams;
    handler?: HandlerParams;
    inputs?: string[];
    path?: string[];
    services?: ServiceClass[];
}
export declare class Factory {
    private params;
    private injector;
    constructor(params: FactoryParams);
    getServiceParams(target: Function, error?: Error | FuncException): any[];
    getMethodServiceParams(instance: object, methodName: string, error?: Error | FuncException): any[];
    private resolveServiceParams;
    private injectedTokens;
    private paramTypes;
    private resolveService;
    private resolveInjectedToken;
}
